<h1>Shopping Cart</h1>

<div class="shopping-cart">
  <%= render "shopping_cart" %>
  
  <h4 class="text-right">Total Number of Items: <span style="color: green"><%= current_order.order_items.size %></span></h4>
  <h4 class="text-right">Total Price: <span style="color: green"><%= number_to_currency current_order.subtotal, :unit => "€" %></span></h4>
  
  <%= link_to "Checkout", new_payment_path, class: "btn btn-primary pull-right" %>  
</div>

